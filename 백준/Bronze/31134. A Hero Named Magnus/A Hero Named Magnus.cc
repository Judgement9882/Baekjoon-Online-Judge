#include <bits/stdc++.h>
using namespace std;
int main() {
	cin.tie(0);
	ios::sync_with_stdio(0);
	int T; cin >> T;
	while (T--) {
		long long x; cin >> x;
		cout << 2 * x - 1<< "\n";
	}
	return 0;
}
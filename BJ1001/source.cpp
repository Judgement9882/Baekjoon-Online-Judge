#include <iostream>

using namespace std;

int main() {

    // input output speed considering
    cin.tie(0);
    ios_base::sync_with_stdio(0);

    int a, b;
    cin >> a >> b;
    cout << a - b;


    return 0;
}
#include <bits/stdc++.h>

using namespace std;

int main() {

	ios::sync_with_stdio(0);
	cin.tie(0);

	int N;
	cin >> N;
	if (N == 0) cout << "YONSEI";
	else if (N == 1) cout << "Leading the Way to the Future";
	
	return 0;
}